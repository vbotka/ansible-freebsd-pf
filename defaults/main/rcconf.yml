---

# pf_rules: /etc/pf.conf
# pf_flags: ''
pf_rcconf: []
#  - {regexp: pf_rules, line: "{{ pf_rules }}"}
#  - {regexp: pf_flags, line: "{{ pf_flags }}"}

# pf_log_logfile: /var/log/pflog
# pf_log_flags: ''
pf_log_rcconf: []
#  - {regexp: pflog_logfile, line: "{{ pf_log_logfile }}"}
#  - {regexp: pflog_flags, line: "{{ pf_log_flags }}"}

# Defaults
#
# /etc/defaults/rc.conf:pf_enable="NO"					# Set to YES to enable packet filter (pf)
# /etc/defaults/rc.conf:pf_rules="/etc/pf.conf"				# rules definition file for pf (nonexistent
# /etc/defaults/rc.conf:pf_program="/sbin/pfctl"			# where the pfctl program lives
# /etc/defaults/rc.conf:pf_flags=""				    	# additional flags for pfctl
# /etc/defaults/rc.conf:pf_fallback_rules_enable="NO"			# fallback if loading ruleset fails
# /etc/defaults/rc.conf:pf_fallback_rules="block drop log all"		# rules to load on pf ruleset failure
# /etc/defaults/rc.conf:#pf_fallback_rules="block drop log all
# /etc/defaults/rc.conf:pf_fallback_rules_file="/etc/pf-fallback.conf"	# rules file on ruleset failure
#
# /etc/defaults/rc.conf:pflog_enable="NO"				# Set to YES to enable packet filter logging
# /etc/defaults/rc.conf:pflog_logfile="/var/log/pflog"  		# where pflogd should store the logfile
# /etc/defaults/rc.conf:pflog_program="/sbin/pflogd"    		# where the pflogd program lives
# /etc/defaults/rc.conf:pflog_flags=""		      	      		# additional flags for pflogd

# EOF
